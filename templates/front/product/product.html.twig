{% extends 'front/layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-6">

            </div>
            <div class="col-sm-12 col-md-6">
                <h1>{{ product.name }}</h1>
                <span>{{ product.price }}</span>
            </div>
        </div>
    </div>
{% endblock %}